pandas>=1.3.0
numpy>=1.20.0
scipy>=1.7.0
Pyomo>=6.0.0
arch>=5.0.0
matplotlib>=3.0.0
# QuantLib-Python is optional and more complex to set up; for the case study,
# we can implement Black-Scholes directly or use simpler models first.
# If QuantLib is needed later:
# QuantLib>=1.20 